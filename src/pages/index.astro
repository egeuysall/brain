---
import DocsApp from "@/components/docs-app.astro"
import { getDocsView } from "@/lib/docs"

const { currentDoc, rendered, sidebarDocs } = await getDocsView()
---

<DocsApp
  currentDoc={currentDoc}
  rendered={rendered}
  sidebarDocs={sidebarDocs}
/>
